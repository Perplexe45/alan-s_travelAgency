A Composer library used by internal plugins to:
   1) update themselves using packages from IONOS Cloud;
   2) retrieve configurations from IONOS Cloud static resources;
   3) retrieve values from global WP options.